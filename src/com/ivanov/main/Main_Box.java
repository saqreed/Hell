/*
package com.ivanov.main;


import com.ivanov.box.Box;
import com.ivanov.box.BoxProcessor;
import com.ivanov.storage.Storage;
import com.ivanov.storage.StorageProcessor;

public class Main_Box {
    public static void main(String[] args) {
        // Задача 6.1.1: Обобщенная коробка
        Box<Integer> intBox = new Box<>();
        intBox.put(3);
        BoxProcessor.processBox(intBox);

        // Задача 6.1.2: Хранилище
        StorageProcessor.processIntegerStorage(new Storage<>(null), 0);
        StorageProcessor.processIntegerStorage(new Storage<>(99), -1);
        StorageProcessor.processStringStorage(new Storage<>(null), "default");
        StorageProcessor.processStringStorage(new Storage<>("hello"), "hello world");
    }
}
*/